---
"@snapdragon-ai/gateway": patch
"@snapdragon-ai/sd": patch
---

Add durable unregister controls for gateway agent runtimes and worker records
across the TypeScript facade, Rust IPC client, REST facade, and `sd gateway`
operator commands.
