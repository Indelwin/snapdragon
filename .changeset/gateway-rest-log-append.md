---
"@snapdragon-ai/gateway": patch
---

Add REST support for appending gateway logs and validate agent runtime
descriptors before registration so external adapters fail clearly before
dashboard/UI work depends on them.
