---
"@snapdragon-ai/sd": minor
---

Add `sd gateway jobs acquire`, `complete`, and `fail` plus `sd gateway logs
append` so local adapters can claim work, report progress, and finish or fail
jobs from the CLI.
