---
'@snapdragon-ai/sd': patch
---

Ship a pinned, bounded-cache Ink renderer in the CLI package, replace the incompatible mouse plugin with an owned streaming input adapter, and add build/memory diagnostics plus real-renderer reliability coverage.
