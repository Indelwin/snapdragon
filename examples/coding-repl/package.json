{
  "name": "@snapdragon/example-coding-repl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -b tsconfig.build.json --pretty false"
  },
  "dependencies": {
    "@snapdragon/agent": "0.1.0",
    "@snapdragon/host": "0.1.0"
  }
}
