# @snapdragon-ai/sd

Reserved package for the batteries-included Snapdragon code agent.

The `sd` binary is intentionally a placeholder while the full agent is split
out into a compact default package. Use `@snapdragon-ai/repl` for the minimal
coding REPL agent until this package grows its TUI, memory, profile, and
tooling defaults.
