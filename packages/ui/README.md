# @snapdragon-ai/ui

Renderer-neutral UI ECS primitives for Snapdragon agents.

This package stores UI descriptors and component state as JSON-serializable data.
Renderers such as Ink, web, or native shells register their own trusted rendering
code outside the UI world.
