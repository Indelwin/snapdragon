# @snapdragon-ai/config

Typed resolved configuration contracts for Snapdragon runtimes.

This package intentionally does not read files, inspect environment variables,
or manage provider tokens. Agents own discovery and pass resolved values into
the generic Snapdragon packages.
