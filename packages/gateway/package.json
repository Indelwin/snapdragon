{
  "name": "@snapdragon-ai/gateway",
  "version": "0.1.1",
  "description": "Embeddable Snapdragon gateway contracts, client API, and inline harness.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "node --test --import tsx test/*.test.ts",
    "test:reliability:gateway": "npm run build && npm run build --workspace @snapdragon-ai/learn && cargo test --manifest-path ../../Cargo.toml -p snapdragon-gateway-core -p snapdragon-gateway-daemon -- --test-threads=1 && node --test --import tsx test/reliability.test.ts test/pi-reliability.test.ts && node --test --import tsx ../sd/test/gateway-command.test.ts",
    "typecheck": "tsc -b tsconfig.build.json --pretty false"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  },
  "license": "MIT"
}
